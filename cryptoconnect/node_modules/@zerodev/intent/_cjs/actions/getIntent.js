"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getIntent = getIntent;
const deepHexlify_js_1 = require("../utils/deepHexlify.js");
async function getIntent(client, parameters, version) {
    const { gasToken, ...rest } = parameters;
    const parametersWithVersion = {
        ...rest,
        gasTokens: gasToken,
        version,
    };
    const intent = (await client.request({
        method: "ui_getIntent",
        params: [(0, deepHexlify_js_1.deepHexlify)(parametersWithVersion)],
    }));
    return intent;
}
//# sourceMappingURL=getIntent.js.map