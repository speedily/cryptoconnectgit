import { OfflineSigner } from "@cosmjs/proto-signing";
import { SigningStargateClient, SigningStargateClientOptions } from "@cosmjs/stargate";
import { Tendermint34Client } from "@cosmjs/tendermint-rpc";
import { BankModule } from "./modules/bank";
import { EmissionsModule } from "./modules/emissions";
export declare class AlloraChainClient {
    private endpoint;
    private queryClient;
    private signer?;
    private signingClient?;
    bank: BankModule;
    emissions: EmissionsModule;
    static init(endpoint: string, signer?: OfflineSigner, signingClientOptions?: SigningStargateClientOptions): Promise<AlloraChainClient>;
    protected constructor(tmClient: Tendermint34Client, endpoint: string, signingClient?: SigningStargateClient);
    connect(newSigner: OfflineSigner, signingClientOptions?: SigningStargateClientOptions): Promise<AlloraChainClient>;
}
export default AlloraChainClient;
//# sourceMappingURL=allora-chain-client.d.ts.map